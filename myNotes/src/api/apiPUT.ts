//Function to PUT (update) a note in the api

/* import { updateNote } from "../interfaces/interface";
import { BASE_URL } from "./apiURL";

export async function API_PUT(id: string, updatenote: updateNote) {
    
} */